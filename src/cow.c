struct cow * new_cow() {

    /*
    calloc() automatically sets all allocated bytes to zero
    void * calloc(size_t number_of_items, size_t memory_size);
    */
    struct cow * cow = calloc(1, sizeof * cow); 



}


int main() {

    return 0;

}
